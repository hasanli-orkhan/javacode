spring:
  liquibase:
    enabled: false
  datasource:
    url: jdbc:postgresql://db:5432/javacode
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true