spring:
  application:
    name: javacode
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:db/initDB.sql