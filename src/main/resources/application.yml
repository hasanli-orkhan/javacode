spring:
  liquibase:
    enabled: false
  datasource:
    url: jdbc:postgresql://app-db:5432/javacode
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: none