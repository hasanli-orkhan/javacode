spring:
  liquibase:
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5434/javacode
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true